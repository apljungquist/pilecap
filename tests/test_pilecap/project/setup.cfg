[metadata]
name = pilecap_sample_project
version = 0.1.2
description = A sample project for testing
classifiers = Private :: Do not upload to PyPI

[options]
install_requires =
    canaria-domestica-red

[options.extras_require]
cli = fire >=0.3
all =
    fire >=0.4
    pluggy